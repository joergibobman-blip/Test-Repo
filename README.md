# Neon Tetris

A simple browser-based Tetris game with neon visuals, arrow-key controls, a live score counter, and sound effects.

## Run locally

Open [index.html](index.html) in a browser, or serve the folder with a simple static server such as:

```bash
python -m http.server 8000
```

Then visit http://localhost:8000/.
